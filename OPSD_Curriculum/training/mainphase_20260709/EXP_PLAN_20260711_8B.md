# 8B Experiment Plan — 2026-07-11

**목표:** 4B에서 확인된 (1) **방향×재분배 2×2 시너지**, (2) **k=2 스윗스팟 용량반응(inverted-U)** 이 8B 스케일에서 재현되는지 확인.

프로토콜: `configs/full_8b_h200.yaml` (Qwen3-8B, context OFF/1024, fixed teacher, jsd_clip 0.06, allow_duplicate_pids=True), pool 28771 → T≈900 step. eval = 5벤치(MATH500·Minerva·AIME24·AIME25·HMMT25), no_thinking, val_n 12(AIME/HMMT)/1(MATH/Minerva).

---

## 현재 상태 (2026-07-11)

| 위치 | arm | 의미 | 상태 |
|---|---|---|---|
| H100 GPU0,1 | contsubj_k1 | continuous-late, k=1 | **RUNNING** (~step1/900, ETA ~13h) |
| H100 GPU2,3 | benchsubj_k1 | discrete-late, k=1 | **RUNNING** |
| H200 C-03 | benchsubj_k2 | discrete-late, k=2 (결합) | **RUNNING** (~28h 남음) |

## 이미 8B에 있음 → 재실행 X

| arm | 위치 | 비고 |
|---|---|---|
| **difficulty-only** | `full_cond2_diff_manifest_once_h200` (ckpt-900), eval `cond2_diff_step900_nonthink` | pool 28771·step900 = **mainphase와 동일 pool** → 재사용. ⚠️ config(ctx OFF·fixed teacher)가 mainphase와 동일한지 확인 후 표에 인용. 불일치 시에만 재실행. |
| **base 8B** | eval `base_qwen3_8b_nonthink` | 완료. 공짜 reference. |
| (old subject: cond3_ours / subjslack) | `full_cond3_ours_*`, `full_cond_*_subjslack_h200` | **구(舊) 구성**(benchsubj/contsubj 방향 아님). 참고용, 표엔 미포함. |

## 8B에서 안 함

- **shuffle(random subject order)**: 8B 미실행(4B만: `cliff4b_main_shuffle` 43.4). 매니페스트 `cond4_shuffle_seed0`(28771) 준비됨. → **선택적 reference**.

---

## 8B 2×2 진행 매트릭스

| Direction \ k | k=1 | k=2 | k=3 |
|---|---|---|---|
| **Continuous-late** | contsubj_k1 · RUNNING | **contsubj_k2 · TODO** ⭐ | — |
| **Discrete-late** | benchsubj_k1 · RUNNING | benchsubj_k2 · H200 RUNNING | **benchsubj_k3 · TODO** |

## 해야 할 학습 (New) — 딱 2개

- **[Tier1] contsubj_k2** — 2×2의 빠진 4번째 칸. 시너지 주장의 필수. (매니페스트 준비·push됨)
- **[Tier2] benchsubj_k3** — 용량반응 재현(k=3 과함 확인). (매니페스트 준비·push됨)
- **[Tier3, 선택] shuffle** — "과목 순서가 중요"의 하한 대조. (아래 prep 필요)

---

## 명령어

### 공통 env (tmux 매 창 최초 1회)
```bash
export REPO=/data1/lamilab/jimin/opsd_repo WORK=/data1/lamilab/jimin/_run
export HF_HOME=$WORK/hf ENV_PY=/data1/lamilab/jimin/envs/opsd/bin/python
cd $REPO && git pull && cd OPSD_Curriculum/training/mainphase_20260709
```

### Wave 2 — H100 GPU 0–3 비면 (contsubj_k2 + benchsubj_k3)
```bash
( CUDA_VISIBLE_DEVICES=0,1 PORT=13100 ./train_mainphase8b.sh contsubj_k2 \
  && CUDA_VISIBLE_DEVICES=0,1 ../h100_port/eval_cliff8b.sh contsubj_k2 ) \
  > $WORK/logs/8b_contsubj_k2.log 2>&1 &
echo ">> contsubj_k2 (GPU0,1) PID $!"

( CUDA_VISIBLE_DEVICES=2,3 PORT=13200 ./train_mainphase8b.sh benchsubj_k3 \
  && CUDA_VISIBLE_DEVICES=2,3 ../h100_port/eval_cliff8b.sh benchsubj_k3 ) \
  > $WORK/logs/8b_benchsubj_k3.log 2>&1 &
echo ">> benchsubj_k3 (GPU2,3) PID $!"
```

### (선택) Tier3 shuffle — prep 후 실행
prep(scratch에서 1회, Claude가 실행·push 예정):
```bash
# cp stages_cond4_shuffle_seed0.json → mainphase_20260709/stages_shuffle.json, commit+push
```
실행(H100):
```bash
( CUDA_VISIBLE_DEVICES=0,1 PORT=13100 ./train_mainphase8b.sh shuffle \
  && CUDA_VISIBLE_DEVICES=0,1 ../h100_port/eval_cliff8b.sh shuffle ) \
  > $WORK/logs/8b_shuffle.log 2>&1 &
```

### 모니터링 (아무 터미널)
```bash
W=/data1/lamilab/jimin/_run
for a in contsubj_k1 benchsubj_k1 contsubj_k2 benchsubj_k3; do
  [ -f $W/logs/8b_${a}.log ] || continue
  echo "== $a =="; tr '\r' '\n' < $W/logs/8b_${a}.log | grep -aE '[0-9]+/[0-9]+ \[' | tail -1
done
nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv,noheader
```

---

## 스케줄 (런당 ~15h: 학습 13h + eval)

| Wave | GPU0,1 | GPU2,3 | 완료 | 산출 |
|---|---|---|---|---|
| 1 (진행중) | contsubj_k1 | benchsubj_k1 | ~15h | 2×2 상단 |
| 2 | contsubj_k2 | benchsubj_k3 | ~30h | **2×2 완성 + 용량반응** |
| H200(병렬) | benchsubj_k2 | | ~28h | 2×2 우하단 |
| 3 (선택) | shuffle | — | ~45h | reference |

핵심 결과(2×2 + dose-response)는 **~30h 뒤 완성**.

---

## 검증 To-do (결과 나온 뒤)

- [ ] **cond2_diff config 확인** — mainphase와 동일 프로토콜(ctx OFF/1024·fixed teacher)인지. 같으면 diff reference로 인용, 다르면 main_diff를 mainphase 프로토콜로 재실행.
- [ ] 8B 2×2 완성 후: 방향효과(bench_k1−cont_k1), 결합효과(bench_k2−cont_k1), 조건부 재분배효과(bench_k2−bench_k1) 계산 → 4B 시너지 패턴 재현 여부 판정.
- [ ] (4B) seed 재현: s42(52.2, done) + s45(H200 진행중) → 52.2 run-variance 확인. 필요 시 s43/44 추가.
- [ ] (별건) subject-orientation 정당성 검증 워크플로(`w6miuq86q`) 결과 반영 → 논문 프레이밍 확정.
